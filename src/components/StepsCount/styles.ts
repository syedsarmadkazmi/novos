import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    paddingTop: 100,
    paddingHorizontal: 50,
  },
  progress: {
    paddingBottom: 40,
  },
  label: {
    fontSize: 20,
    paddingBottom: 20,
  },
  stepsText: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  smallText: {
    fontSize: 15
  }
});
